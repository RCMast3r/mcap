@PACKAGE_INIT@

if (NOT ${lz4_FOUND})
    find_dependency(lz4)
endif()

if(NOT ${zstd_FOUND})
    find_dependency(zstd)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/mcapTargets.cmake")

check_required_components(mcap)